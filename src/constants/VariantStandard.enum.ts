/**
 * Novo padrão de variantes a ser utilizado nos componentes.
 *
 * @remarks
 * Este enum é para uso interno do design system.
 * Tenha cautela e consulte a documentação de cada componente para verificar quais variantes
 * são suportadas antes de utilizá-las.
 *
 * @internal
 */
export enum VariantStandard {
  DEFAULT = 'default',
  HIGHLIGHT = 'highlight',
  SUCCESS = 'success',
  WARNING = 'warning',
  CRITICAL = 'critical',
}
