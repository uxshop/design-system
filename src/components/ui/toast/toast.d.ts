declare module 'toast' {
  export interface toast {
    protocol?: string;
    hostname?: string;
    pathname?: string;
  }
}

export interface toast {
  protocol?: string;
  hostname?: string;
  pathname?: string;
}
